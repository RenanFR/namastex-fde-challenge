# Copie para .env e preencha. O .env está no .gitignore e nunca deve ser commitado.
# Rode antes do agente:  set -a && source .env && set +a
ANTHROPIC_API_KEY=

# Opcionais
# AUTOSEGURO_MODELO=claude-opus-5
# AUTOSEGURO_ESFORCO=medium
# AUTOSEGURO_ENCAMINHAMENTO_AUTOMATICO=true
# AUTOSEGURO_MASCARAR_PII=true
